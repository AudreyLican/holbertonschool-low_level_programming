#include "main.h"

/**
* int _abs - computes the absolute value of the integer
*
* @int : the parameter of this function is a integer
*/
int _abs(int a)
{
	if (a >= 0)
		return(a);
	else
	{
		return(a * -1);
	}
}

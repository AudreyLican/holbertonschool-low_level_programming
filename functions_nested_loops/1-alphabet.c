#include <stdio.h>
#include <_putchar.h>
#include <main.h>

/**
* Description: print the alphabet in lowercase
*/
void	print_alphabet(void)
{
	char	lowcase;

	lowcase = 'a';
	while (lowercase < 'z')
	{
		_putchar(lowcase);
		lowcase++;
	}
	_putchar('\n);

	return (0);
}

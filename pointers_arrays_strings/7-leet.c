#include "main.h"

/**
* *leet - function that encodes a string into 1337
* @str: string
*
* Return: string encoded
*/

char *leet(char *str)
{
	int	i = 0;
	int	j = 0;
	char	c[] = {'a', 'e' , 'o', 't', 'l'};
	char	d[] = {'4', '3', '0', '7', '1'};

	while (str[i] != '\0')
	{
		while (j <)
		if (str[i] == c || str[i] == d)
		{
			str[i] = ;
		}
	}
}
